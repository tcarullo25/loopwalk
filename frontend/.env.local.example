# Public, URL-restricted Mapbox token for the browser (map tiles + geocoding).
# This is NOT the backend's secret token — that one lives in dotnet user-secrets.
# Create one at https://account.mapbox.com/access-tokens/ and restrict it by URL.
VITE_MAPBOX_TOKEN=pk.your_public_token_here
